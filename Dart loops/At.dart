void main() {
  int counter = 1;
  // Primeiro define a variavél no scoopo, relativamente parecido com C;
  
  while (counter <= 10) {
    print(counter);
    counter++;
  }
  // Após isso, vira um Loop semalhante a um while de C++
}